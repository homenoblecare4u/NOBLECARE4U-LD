# Noblecare4u Leads Dashboard Environment Variables
# Server-side MongoDB Connection (Connects to the same database as the main website)
MONGODB_URI=

# Authentication Secret (Used for signing secure session cookies)
AUTH_SECRET=

# Administrator Email (Authorized login email)
ADMIN_EMAIL=

# Administrator Password Hash (Bcrypt hash of the admin password)
ADMIN_PASSWORD_HASH=
